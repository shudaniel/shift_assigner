<link rel='stylesheet' href='fullcalendar/fullcalendar.css' />
<script src='lib/jquery.min.js'></script>
<script src='lib/moment.min.js'></script>
<script src='fullcalendar/fullcalendar.js'></script>

<script>

  $(document).ready(function() {

    // page is now ready, initialize the calendar...

    $('#calendar').fullCalendar({
      // put your options and callbacks here
      defaultView: "agendaWeek",
      events: "/shifts.json?calendar_id=<%=@calendar.id%>"
    });

  });

</script>

<p id="notice"><%= notice %></p>

<h1><%= @calendar.name %> Shifts</h1>


<div class="panel panel-default">
  <div class="panel-heading">
    <div class="row">
      <div class="col-xs-2">Title</div>
      <div class="col-xs-3">Description</div>
      <div class="col-xs-2">Start Time</div>
      <div class="col-xs-2">End Time</div>
      <div class="col-xs-3"></div>
    </div>
  </div>
  <div class="panel-body">
    <% @calendar.shifts.each do |shift| %>
      <div class="row">
        <div class="col-xs-2"><%= shift.title %></div>
        <div class="col-xs-3"><%= shift.description %></div>
        <div class="col-xs-2"><%= Shift.format_datetime(shift.start_time) %></div>
        <div class="col-xs-2"><%= Shift.format_datetime(shift.end_time) %></div>
        <div class="col-xs-1"><%= link_to 'Show', shift %></div>
        <div class="col-xs-1"><%= link_to 'Edit', edit_shift_path(shift) %></div>
        <div class="col-xs-1"><%= link_to 'Delete', shift, method: :delete, data: { confirm: 'Are you sure?' } %></div>
      </div>
    <% end %>
  </div>
</div>


<br>

<%= link_to 'New Shift', new_shift_path(calendar_id: @calendar.id) %> |
<%= link_to 'Edit', edit_calendar_path(@calendar) %> |
<%= link_to 'Back', calendars_path %>

<br>

<div id="calendar"></div>

