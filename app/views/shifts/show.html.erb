<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @shift.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @shift.description %>
</p>

<p>
  <strong>Start time:</strong>
  <%= Shift.format_datetime(@shift.start_time) %>
</p>

<p>
  <strong>End time:</strong>
  <%= Shift.format_datetime(@shift.end_time) %>
</p>

<%= link_to 'Edit', edit_shift_path(@shift) %> |
<%= link_to 'Back', calendar_path(@shift.calendar) %>
